# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Datos.index()
POST    /crear	                    controllers.Datos.crear()
GET		/listar						controllers.Datos.listar()
POST	/actualizar					controllers.Datos.actualizar()
POST	/eliminar					controllers.Datos.eliminar()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
